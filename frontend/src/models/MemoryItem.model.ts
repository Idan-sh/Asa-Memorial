export interface MemoryItemData {
    id: string;
    first_name: string;
    last_name: string;
    nickname: string;
    relation: string;
    message: string;
    contact_email: string;
    images: Buffer[];
}